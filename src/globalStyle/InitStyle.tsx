import { createGlobalStyle } from 'styled-components';

export const InitStyle = createGlobalStyle`
  // 여기에 global style css 지정하기
  body {
    padding: 0;
    margin: 0;
  }
  p, h1, h2, h3 {
    padding: 0;
    margin: 0;
  }
`;
